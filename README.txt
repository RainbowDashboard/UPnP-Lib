Import UPnP and use this command:

For open port use:
UPnP.port2open(port, name)

port => the port you want
name => the name you want to the port forward

For retrieve you public IP use:
UPnP.show_IP()
